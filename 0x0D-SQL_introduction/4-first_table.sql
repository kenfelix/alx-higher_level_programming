-- Creates a table called first_table in the current database in your MySQL server.
-- Do nothing If the table first_table already exists.
CREATE TABLE IF NOT EXISTS first_table (id INT, name VARCHAR(256))
